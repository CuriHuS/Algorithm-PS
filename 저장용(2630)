import math
N=int(input())
k=math.sqrt(N) # k값확인
graph=[]
white=0
blue=0
for i in range(N):
    graph.append(list(map(int,input().split())))
while k>1:
    #2사분면
    truth=2
    for y in range(2**(k-1)):
        for x in range(2**(k-1)):
            if truth==2:
                graph[y][x]
